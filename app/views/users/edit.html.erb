<div class="container text-center">
	<div class="form-horizontal col-md-4 col-md-offset-4">
		<%= simple_form_for @user, controller: 'edit', action: 'post' do |f| %>
			<%= f.input :first_name %>
			<%= f.input :last_name %>
			<%= f.input :bio %>
			<%= f.submit %>
		<% end %>
	</div>
</div>